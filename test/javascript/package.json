{
  "name": "live-transcribe-ts",
  "version": "1.0.0",
  "description": "Real-time live transcription using Vosk – TypeScript + Bun FFI",
  "type": "module",
  "scripts": {
    "start": "bun run live_transcribe.ts",
    "start:device": "bun run live_transcribe.ts --list-devices"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
